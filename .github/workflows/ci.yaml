name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      # The same two steps the deploy workflow runs, without the deploy.
      # --frozen-lockfile fails when package.json and bun.lock disagree.
      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build
